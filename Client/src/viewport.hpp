#pragma once 

namespace Viewport {
    extern unsigned int height;
    extern unsigned int width;

    extern float global_scalefactor;
};

namespace Window {
    extern unsigned int height;
    extern unsigned int width;
}